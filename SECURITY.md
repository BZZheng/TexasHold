# 好友德州扑克安全模型

## 服务端权威边界

浏览器是不可信客户端，只能发送有限的操作意图。服务端从认证会话中确定玩家身份，并自行计算当前行动者、合法操作、加注范围、筹码、底池、牌堆、公共牌和胜负。

- `game:action` 只允许 `action`、`amount`、`handId`、`actionToken` 四类字段。
- `game:time-extension` 只允许 `handId` 与当前 `actionToken`；服务端校验行动者、截止时间、剩余筹码和本回合使用次数，并自行扣除 500 筹码、增加 60 秒。
- 客户端提交 `cards`、`deck`、`community`、`stack`、`players` 等牌局状态会被直接拒绝。
- 每次轮到玩家时生成一次性的随机 `actionToken`；行动后立即作废，旧包、重复点击和延迟重放都不能在后续回合执行。
- 远程加入者一律先成为观战者。客户端伪造 `mode: "player"` 不能绕过选座和房主批准流程，也不会获得初始筹码。
- 洗牌使用 Node.js `crypto.randomInt` 的 Fisher–Yates 洗牌。牌堆只存在于服务端控制的内存和 `runtime-rooms.json` 恢复快照中，绝不下发给客户端。
- 服务端每次变更后检查 52 张牌唯一性、筹码非负与筹码总量守恒。完整性异常时停止该操作，不向客户端发布伪造状态。
- 每位玩家收到独立裁剪后的牌局视图：牌局中只有本人收到自己的两张底牌；观战者与其他玩家不会收到牌值；摊牌时仅公开未弃牌玩家的手牌。

## 会话与网络防护

- 新登录会话使用 `HttpOnly; SameSite=Strict` Cookie，前端脚本不能读取新令牌。HTTPS 部署同时使用 `Secure`；本地 HTTP 联调因浏览器限制关闭 `Secure`。
- 旧版 `localStorage` 令牌只用于一次迁移，成功恢复会话后立即删除。
- 会话令牌以 SHA-256 摘要落盘，`data/texashold.json` 不保存可直接使用的原始令牌。`data/runtime-rooms.json` 包含进行中牌局的牌堆和手牌，使用原子替换及 `0600` 权限保存，必须按敏感数据保护。
- Socket 在握手和每次消息处理时都重新验证会话；退出登录会主动断开使用同一会话的 Socket。
- 生产环境按 `APP_ORIGINS` 精确校验 WebSocket `Origin`，并拒绝无来源握手。
- HTTP、登录、注册、Socket 握手、牌局操作和聊天分别限流；Socket 单条消息上限 16 KiB，并关闭消息压缩。
- 安全响应头包含 CSP、禁止 iframe、禁止 MIME 嗅探、最小权限 `Permissions-Policy`；生产 HTTPS 下启用 HSTS。
- 安全拒绝和限流写入结构化日志，但不记录密码、会话令牌、聊天正文或手牌。

## 远端部署检查

1. 应用端口默认只绑定回环地址，通过 HTTPS 反向代理对外提供服务；防火墙只开放必要端口。
2. 远程管理入口应限制来源、禁用密码登录并使用独立密钥。
3. 上线时必须把允许来源精确设置为实际访问域名：

   ```bash
   docker compose \
     --project-name texas-holdem \
     --env-file deploy/production.env \
     -f docker-compose.production.yml \
     up -d --build
   ```

4. 容器使用非 root 用户、只读根文件系统、移除全部 Linux capabilities、禁止提权，并限制 CPU、内存和进程数。
5. `/data` 是唯一可写持久卷。定期备份并把主机目录权限限制给部署用户；不要把数据文件提交到 Git。
6. 定期更新基础镜像与 npm 依赖，并在更新后执行 `npm run check`。
7. 若服务完全公开且担心流量型 DDoS，需要在应用之前增加流量防护或反向代理/WAF；单个 Node 容器无法抵挡带宽耗尽攻击。

任何纯 HTTP 公网来源都不提供传输加密。应用层仍会拒绝改牌、改筹码、越权看牌和重放操作，但同一不可信网络中的攻击者可能窃听或劫持 HTTP 会话；这部分风险只有 HTTPS 能消除。

## 外部归档

- 应用主机只保留恢复牌局所需的热数据和有界日志，外部归档端通过受限 SSH 主动拉取；归档端离线不会让线上游戏停止。
- 归档专用密钥使用来源地址、强制命令和 `restrict` 限制，只能执行固定导出命令；真实主机、用户名、密钥路径和归档根目录仅存在于未跟踪的环境文件中。
- 归档目录权限为 `0700`，数据文件权限为 `0600`；恢复前先停止写入进程并校验快照。

## 数据库与公开仓库边界

- 应用服务没有在线外部数据库，也没有数据库连接串或数据库账号。账号与可恢复牌局状态由服务端写入 `DATA_DIR` 下的本地 JSON 文件；NAS 拉取任务另行维护离线 SQLite 分析库。JSON、逐手底牌分析与 SQLite 都是敏感运行数据，整个 `data/` 和 NAS 归档目录不得进入 Git 或构建上下文。
- 单元测试只在系统临时目录创建隔离数据，测试完成后删除。仓库中不得提交开发者本机数据、线上数据、备份、日志、复制游标或数据库转储。
- 如需新增数据库脚本，公开仓库只允许提交可复现的建库建表脚本 `database/schema/**/*.sql`，以及不含真实用户信息的测试数据 `database/fixtures/**/*.sql` 或 `tests/fixtures/**/*.sql`。真实数据库文件、转储、连接 URL、主机、库名、账号和密码必须留在未跟踪的环境中。
- `.env.example` 与 `*.env.example` 只能包含本地回环地址、保留示例域名、空值或明确占位符；它们不能复制任何部署环境的值。
- `/api/health` 等未鉴权接口只能返回健康布尔值、容量和时间状态，不得返回 `DATA_DIR`、归档目录、主机名或连接配置。
- `npm run privacy:check` 会检查待公开文件；推送前还应运行 `npm run privacy:check:history`，检查相对 `origin/main` 的所有待推送提交快照及 Git 身份元数据。
- 只在内容发生变化时创建快照，默认保留 90 天；恢复时必须先停止游戏容器。

## 明确边界

这些措施能阻止普通玩家通过 DevTools、改前端代码、伪造 Socket 包、重放操作或修改请求字段来改牌和改筹码。若攻击者已经取得主机管理员权限、Node 进程内存读取权限或部署密钥，应用层无法保证牌局秘密；此时必须依赖主机补丁、密钥管理、最小端口暴露和入侵响应。
